import React from 'react';

const CorrectAnswer = ({ handlerCorrectAnswer }) => {
    // console.log(handlerCorrectAnswer)
    return (
        <div>
            
        </div>
    );
};

export default CorrectAnswer;